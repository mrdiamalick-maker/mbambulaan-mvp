"use client";

import { useMemo, useState, type ReactNode } from "react";
import { quayPosts, type QuayPost } from "@/data/ministryControlTowerData";
import type { DossierChannel, DossierOperationnel, DossierType, DossierWorkStatus } from "@/lib/ministryOperationalDossiers";
import { DataTrustBadge, primaryButton, secondaryButton, StatusBadge } from "./MinistryControlTowerParts";

const statusLevel: Record<DossierWorkStatus, "normal" | "surveillance" | "urgent"> = { Nouveau: "normal", "À traiter": "surveillance", "En attente": "surveillance", Bloqué: "urgent", Terminé: "normal" };
const typeCode: Record<DossierType, string> = { Vérification: "VER", Incident: "INC", Financement: "FIN", Rapport: "RAP", Note: "NOT" };
const workOrder: Record<DossierWorkStatus, number> = { Bloqué: 0, "À traiter": 1, Nouveau: 2, "En attente": 3, Terminé: 4 };
type StatusFilter = "Tous" | DossierWorkStatus;

export function DossierCard({ dossier, onOpen, prominent = false }: { dossier: DossierOperationnel; onOpen: (dossier: DossierOperationnel) => void; prominent?: boolean }) {
  return <button onClick={() => onOpen(dossier)} className={`w-full border bg-white text-left transition-colors hover:border-[var(--mb-ocean-600)] ${prominent ? "border-[var(--mb-ocean-600)]" : "border-[var(--mb-neutral-200)]"}`}>
    <div className="grid gap-3 p-4 sm:grid-cols-[7.5rem_minmax(0,1fr)_auto] sm:items-start">
      <div><span className="inline-flex border border-[var(--mb-neutral-200)] px-2 py-1 font-mono text-[9px] font-bold text-[var(--mb-ocean-600)]">{dossier.id}</span><p className="mt-2 font-mono text-[8px] uppercase tracking-[0.08em] text-[var(--mb-neutral-500)]">{typeCode[dossier.type]} · {dossier.workStatus}</p></div>
      <div><h3 className="text-[13px] font-semibold leading-5 text-[var(--mb-navy-900)]">{dossier.linkedObject}</h3><p className="mt-1 text-[9px] leading-4 text-[var(--mb-neutral-600)]">{dossier.territory} · {dossier.currentOwner}</p><div className="mt-2 flex flex-wrap gap-2"><ChannelBadge channel={dossier.originChannel} /><span className="font-mono text-[7px] uppercase text-[var(--mb-neutral-500)]">{dossier.type} · {dossier.linkedObjectType}</span></div></div>
      <div className="text-left sm:text-right"><StatusBadge level={statusLevel[dossier.workStatus]}>{dossier.workStatus}</StatusBadge><p className="mt-2 max-w-48 text-[9px] font-bold leading-4 text-[var(--mb-ocean-600)]">{dossier.nextAction} →</p></div>
    </div>
  </button>;
}

export function MyDossiers({ dossiers, onOpen }: { dossiers: DossierOperationnel[]; onOpen: (dossier: DossierOperationnel) => void }) {
  const [status, setStatus] = useState<StatusFilter>("Tous");
  const [type, setType] = useState<"Tous" | DossierType>("Tous");
  const [channel, setChannel] = useState<"Tous" | DossierChannel>("Tous");
  const [quay, setQuay] = useState("Tous");
  const filtered = useMemo(() => dossiers.filter((dossier) => dossier.workStatus !== "Terminé" || status === "Terminé").filter((dossier) => status === "Tous" || dossier.workStatus === status).filter((dossier) => type === "Tous" || dossier.type === type).filter((dossier) => channel === "Tous" || dossier.originChannel === channel).filter((dossier) => quay === "Tous" || dossier.quayId === quay).sort((a, b) => workOrder[a.workStatus] - workOrder[b.workStatus] || b.ageDays - a.ageDays), [channel, dossiers, quay, status, type]);
  const waiting = dossiers.filter((dossier) => dossier.workStatus !== "Terminé").length;
  const blocked = dossiers.filter((dossier) => dossier.workStatus === "Bloqué").length;
  const completed = dossiers.filter((dossier) => dossier.workStatus === "Terminé").length;
  return <section className="border-t border-[var(--mb-neutral-200)] bg-[var(--mb-offwhite)] px-4 py-5 sm:px-6 lg:px-8"><div className="mx-auto max-w-[86rem]"><div className="flex flex-wrap items-end justify-between gap-3 border-b border-[var(--mb-neutral-200)] pb-4"><div><p className="font-mono text-[8px] font-bold uppercase tracking-[0.1em] text-[var(--mb-ocean-600)]">Direction régionale</p><h2 className="mt-1 text-[18px] font-semibold text-[var(--mb-navy-900)]">Mon bureau du jour</h2><p className="mt-1 text-[9px] text-[var(--mb-neutral-600)]">Une pile de dossiers priorisée, filtrable, sans logique de tableau de tickets.</p></div><div className="grid grid-cols-3 divide-x divide-[var(--mb-neutral-200)] border border-[var(--mb-neutral-200)] bg-white text-center"><BureauMetric label="À suivre" value={waiting} /><BureauMetric label="Bloqués" value={blocked} /><BureauMetric label="Clos" value={completed} /></div></div><div className="mt-4 flex flex-wrap gap-2"><DossierFilter label="Afficher" value={status} onChange={(value) => setStatus(value as StatusFilter)} options={["Tous", "À traiter", "En attente", "Bloqué", "Nouveau", "Terminé"]} /><DossierFilter label="Type" value={type} onChange={(value) => setType(value as "Tous" | DossierType)} options={["Tous", "Vérification", "Incident", "Financement", "Rapport", "Note"]} /><DossierFilter label="Canal" value={channel} onChange={(value) => setChannel(value as "Tous" | DossierChannel)} options={["Tous", "WhatsApp", "Téléphone", "Poste de quai", "Agent territorial", "Formulaire", "Document"]} /><DossierFilter label="Quai" value={quay} onChange={setQuay} options={["Tous", "kayar", "joal", "saint-louis"]} /></div><div className="mt-4 grid gap-2">{filtered.map((dossier, index) => <DossierCard key={dossier.id} dossier={dossier} onOpen={onOpen} prominent={index === 0} />)}{!filtered.length ? <p className="border border-dashed border-[var(--mb-neutral-200)] bg-white px-4 py-6 text-center text-[10px] text-[var(--mb-neutral-500)]">Aucun dossier dans cette vue.</p> : null}</div></div></section>;
}

export function OperationalDossierPanel({ dossier, onClose, onPrimary, onRelance }: { dossier: DossierOperationnel; onClose: () => void; onPrimary: (dossier: DossierOperationnel) => void; onRelance: (dossier: DossierOperationnel) => void }) {
  const [notes, setNotes] = useState(dossier.notes);
  const [note, setNote] = useState("");
  return <div className="fixed inset-0 z-[100] flex justify-end bg-[var(--mb-navy-900)]/35" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><aside className="h-full w-full max-w-2xl overflow-y-auto border-l border-[var(--mb-neutral-200)] bg-white shadow-[-18px_0_45px_rgba(9,31,48,.12)]"><header className="sticky top-0 z-10 border-b border-[var(--mb-neutral-200)] bg-white px-5 py-5"><div className="flex items-start justify-between gap-4"><div className="min-w-0"><p className="inline-flex border border-[var(--mb-neutral-300)] px-2 py-1 font-mono text-[10px] font-bold text-[var(--mb-navy-900)]">{dossier.id}</p><p className="mt-3 font-mono text-[8px] font-bold uppercase tracking-[0.12em] text-[var(--mb-ocean-600)]">Dossier {dossier.type} · {dossier.linkedObjectType}</p><h2 className="mt-1 text-[20px] font-semibold leading-6 text-[var(--mb-navy-900)]">{dossier.linkedObject}</h2></div><button onClick={onClose} className="h-8 w-8 border border-[var(--mb-neutral-200)] bg-white text-[14px]" aria-label="Fermer">×</button></div><dl className="mt-4 grid gap-2 border-y border-[var(--mb-neutral-200)] py-3 text-[9px] sm:grid-cols-2"><DossierFact label="Canal d’origine" value={dossier.originChannel} /><DossierFact label="Responsable" value={dossier.currentOwner} /><DossierFact label="Statut métier" value={dossier.businessStatus} /><DossierFact label="Sortie attendue" value={dossier.finalOutput} /></dl><div className="mt-3 flex flex-wrap items-center gap-2"><ChannelBadge channel={dossier.originChannel} /><span className="font-mono text-[8px] uppercase text-[var(--mb-neutral-500)]">Travail · {dossier.workStatus}</span><DataTrustBadge level={dossier.pieces.some((piece) => piece.status === "Attendue") ? "declared" : "verified"} source="Niveau déduit des pièces locales disponibles dans ce dossier." /></div><DossierRoute dossier={dossier} /></header><div className="grid gap-0 px-5 pb-5"><DossierDocumentSection title="Poste de quai">{dossier.quayId ? <PosteOfficielPanel poste={quayPosts.find((poste) => poste.quayId === dossier.quayId)} /> : <p className="text-[10px] text-[var(--mb-neutral-500)]">Aucun poste rattaché à ce dossier.</p>}</DossierDocumentSection><section className="border-b border-[var(--mb-neutral-200)] py-4"><p className="font-mono text-[8px] font-bold uppercase text-[var(--mb-ocean-600)]">Prochaine action</p><h3 className="mt-2 text-[14px] font-semibold text-[var(--mb-navy-900)]">{dossier.nextAction}</h3><p className="mt-2 text-[10px] leading-5 text-[var(--mb-neutral-600)]">Cette action met à jour le statut métier existant et ajoute une trace au dossier.</p>{dossier.action !== "none" ? <button onClick={() => onPrimary(dossier)} className={`${primaryButton} mt-3`}>{dossier.nextAction}</button> : <p className="mt-3 text-[9px] font-bold text-[var(--mb-green-600)]">Dossier terminé · aucune action principale</p>}{dossier.workStatus === "Bloqué" ? <button onClick={() => onRelance(dossier)} className={`${secondaryButton} ml-2 mt-3`}>Relancer</button> : null}</section><DossierDocumentSection title="Notes"><form onSubmit={(event) => { event.preventDefault(); if (!note.trim()) return; setNotes((items) => [...items, { id: `note-${Date.now()}`, time: new Intl.DateTimeFormat("fr-FR", { hour: "2-digit", minute: "2-digit" }).format(new Date()), author: "Agent connecté · simulation", text: note.trim() }]); setNote(""); }} className="grid gap-2"><textarea value={note} onChange={(event) => setNote(event.target.value)} rows={2} placeholder="Ajouter une note courte au dossier" className="w-full border border-[var(--mb-neutral-200)] bg-white p-2 text-[10px] outline-none focus:border-[var(--mb-ocean-600)]" /><button className={`${secondaryButton} justify-self-start`}>Ajouter la note</button></form><ol className="mt-3 divide-y divide-[var(--mb-neutral-100)]">{notes.map((item) => <li key={item.id} className="py-2"><p className="text-[9px] leading-4 text-[var(--mb-neutral-700)]">{item.text}</p><p className="mt-1 font-mono text-[7px] text-[var(--mb-neutral-400)]">{item.time} · {item.author}</p></li>)}</ol></DossierDocumentSection><DossierDocumentSection title="Pièces"><div className="grid gap-2 sm:grid-cols-2">{dossier.pieces.map((piece) => <div key={piece.id} className="border border-[var(--mb-neutral-200)] bg-white p-3"><p className="font-mono text-[8px] text-[var(--mb-ocean-600)]">{piece.type.toUpperCase()}</p><p className="mt-1 text-[9px] font-semibold">{piece.label}</p><p className={`mt-2 text-[8px] font-bold ${piece.status === "Disponible" ? "text-[var(--mb-green-600)]" : "text-[#956314]"}`}>{piece.status}</p></div>)}</div></DossierDocumentSection><DossierDocumentSection title="Historique du dossier"><ol className="grid gap-2">{dossier.history.map((entry, index) => <li key={`${entry.time}-${index}`} className="grid grid-cols-[3rem_minmax(0,1fr)] gap-2 border-l border-[var(--mb-ocean-400)] pl-3"><time className="font-mono text-[8px] font-bold text-[var(--mb-ocean-600)]">{entry.time}</time><div><p className="text-[9px] font-semibold">{entry.label}</p><p className="mt-1 text-[8px] text-[var(--mb-neutral-500)]">{entry.author} · <ChannelBadge channel={entry.channel} /></p></div></li>)}</ol></DossierDocumentSection></div></aside></div>;
}

function DossierRoute({ dossier }: { dossier: DossierOperationnel }) {
  const steps = ["Signal reçu", "Dossier ouvert", "Complété", "Vérifié", "Consolidé", "Sortie"];
  const active = dossier.workStatus === "Terminé" ? 5 : dossier.pieces.some((piece) => piece.status === "Attendue") ? 2 : 3;
  return <ol className="mt-4 grid grid-cols-6 gap-1">{steps.map((step, index) => <li key={step} className="min-w-0"><span className={`block h-1 ${index <= active ? "bg-[var(--mb-ocean-600)]" : "bg-[var(--mb-neutral-200)]"}`} /><p className={`mt-1 truncate text-[7px] ${index === active ? "font-bold text-[var(--mb-navy-900)]" : "text-[var(--mb-neutral-500)]"}`}>{step}</p></li>)}</ol>;
}

export function PosteOfficielPanel({ poste }: { poste?: QuayPost }) {
  if (!poste) return null;
  return <div className="py-1"><p className="font-mono text-[8px] font-bold uppercase text-[var(--mb-ocean-600)]">Poste officiel</p><h3 className="mt-1 text-[10px] font-semibold text-[var(--mb-navy-900)]">{poste.name}</h3><p className="mt-1 text-[8px] leading-4 text-[var(--mb-neutral-600)]">{poste.officer} · {poste.phone} · {poste.hours}</p><p className="mt-2 text-[8px] leading-4 text-[var(--mb-neutral-600)]">Relais principal WhatsApp / téléphone et vérification de premier niveau. La validation finale reste régionale.</p></div>;
}

export function ChannelBadge({ channel }: { channel: DossierChannel }) { return <span className="inline-flex items-center rounded-[3px] border border-[var(--mb-neutral-200)] bg-[var(--mb-offwhite)] px-2 py-1 font-mono text-[7px] font-bold uppercase text-[var(--mb-neutral-600)]">{channel}</span>; }
function DossierDocumentSection({ title, children }: { title: string; children: ReactNode }) { return <section className="border-b border-[var(--mb-neutral-200)] py-4"><h3 className="font-mono text-[8px] font-bold uppercase tracking-[0.08em] text-[var(--mb-neutral-500)]">{title}</h3><div className="mt-3">{children}</div></section>; }
function DossierFact({ label, value }: { label: string; value: string }) { return <div className="grid grid-cols-[7rem_minmax(0,1fr)] gap-2"><dt className="text-[var(--mb-neutral-500)]">{label}</dt><dd className="font-semibold text-[var(--mb-navy-900)]">{value}</dd></div>; }
function BureauMetric({ label, value }: { label: string; value: number }) { return <div className="px-3 py-2"><p className="font-mono text-[13px] font-bold text-[var(--mb-navy-900)]">{value}</p><p className="text-[7px] font-bold uppercase tracking-[0.08em] text-[var(--mb-neutral-500)]">{label}</p></div>; }
function DossierFilter({ label, value, options, onChange }: { label: string; value: string; options: string[]; onChange: (value: string) => void }) { return <label className="grid gap-1 font-mono text-[7px] font-bold uppercase text-[var(--mb-neutral-500)]">{label}<select value={value} onChange={(event) => onChange(event.target.value)} className="h-8 min-w-28 border border-[var(--mb-neutral-200)] bg-white px-2 text-[9px] font-semibold normal-case text-[var(--mb-navy-900)]">{options.map((option) => <option key={option}>{option}</option>)}</select></label>; }
